import React from 'react';
import ReactDOM from 'react-dom/client';
import App from "./App.jsx"; // Update the import path for App component
import "./index.css"; // Update the import path for CSS

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
